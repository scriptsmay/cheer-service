# cheer-service Vercel 部署环境变量
# 在 Vercel 项目设置 → Environment Variables 中逐项填写
# 变量名会被 Vercel 自动识别

# ── 数据库（Supabase Postgres） ──
# 必填：从 Supabase 项目设置获取完整连接串
POSTGRES_URI=postgresql://postgres:your_password@db.xxx.supabase.co:5432/postgres?pgbouncer=true

# ── JWT 鉴权 ──
# 必填：生成随机字符串
JWT_SECRET=generate_a_random_secret_here

# ── AI 配置 (OpenAI 兼容标准) ──
# 必填：DeepSeek API
AI_BASE_URL=https://api.deepseek.com/v1
AI_API_KEY=your_api_key_here
AI_MODEL=deepseek-chat

# ── 旧版 Token 鉴权（兼容） ──
AUTH_TOKEN=your_auth_token_here

# ── 用户表（JSON 数组，用于 JWT 登录） ──
# 必填：至少一个 viewer 用户
APP_USERS=[{"username":"viewer","password":"your_password_here","subjectId":"viewer:default"}]

# ── 内容安全 ──
BLOCKED_TERMS=赌博,色情,自杀,暴力,恐怖,毒品,诈骗,传销,政治敏感,反动,违法,违规

# ── 限流 ──
IP_HASH_SALT=generate_a_random_salt_here

# ── Vercel Cron 鉴权（可选：配置后 cron 请求须带 Bearer token） ──
CRON_SECRET=your_cron_secret_here

# ── Upstash Redis（可选：serverless 多实例共享限流计数） ──
# 不配置则回退进程内 Map（单实例足够）
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# ── KPL 数据源（Vercel 部署使用 GitHub raw） ──
KPL_SOURCE=github
KPL_GITHUB_RAW_BASE=https://raw.githubusercontent.com/scriptsmay/kpl_data_daily/main
